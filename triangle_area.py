#A triangle is formed by connecting the top left corner of the bigger square (0, x) with the bottom right corner of the 
#smaller square (x+y, 0) and the top left corner of the smaller square (x, y).
import math
x = float(input("Enter the value of x:"))
y = float(input("Enter the value of y:"))
base = abs(x) + abs(y)
s1 = math.sqrt()